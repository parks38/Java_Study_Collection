## 함수형 프로그래밍 
* 1급시민
  * var 함수를 변수에 선언할수 있어야 함. 
  * 함수를 parameter에 넣을 수 있어야 함. 
  * 함수를 return 할수 있어야 함. 

* 함수 vs. Method 차이 : 클래스 여부 
* @functionalInterface
* support, consumer, predicate, runnable, function
* Optionall Wrapper .ofNullable() .ofNull()
  * orElse(), orElseGet(), orElseThrow 
* stream
  * filter()
  * collect()
  * limit()

* lazy, short curcuit 기법 for 최적화 
* auto boxing, auto unboxing because generic does not allow Primitive types only Reference types 
